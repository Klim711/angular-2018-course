import { CourseClassPipe } from './course-class.pipe';

describe('CourseClassPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseClassPipe();
    expect(pipe).toBeTruthy();
  });
});
